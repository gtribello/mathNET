language: python
python:
  - "3.5"
# command to install dependencies
install: 
  - "pip install PyQt4"
  - "pip install graphviz"

script:
  - python Scripts/build-website.py

after_success:
  - bash deploy.sh

env:
  global:
    - secure:
